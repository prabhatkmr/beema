# Development profile configuration for Beema Kernel

spring:
  devtools:
    restart:
      enabled: true
      additional-paths:
        - src/main/java
        - src/main/resources
      exclude: static/**,public/**
    livereload:
      enabled: true
      port: 35729

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  flyway:
    enabled: true
    clean-on-validation-error: true  # WARNING: Only for dev!

logging:
  level:
    root: INFO
    com.beema: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    io.temporal: DEBUG

# Temporal Worker Configuration (dev mode)
temporal:
  worker:
    enabled: true
    max-concurrent-activities: 5
    max-concurrent-workflows: 5

# Actuator endpoints for development
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
