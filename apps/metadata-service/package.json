{
  "name": "@beema/metadata-service",
  "version": "0.1.0",
  "private": true,
  "description": "Beema Metadata Service - Schema Registry and Field Definitions",
  "scripts": {
    "build": "mvn clean package -DskipTests",
    "test": "mvn test",
    "lint": "mvn checkstyle:check",
    "dev": "mvn spring-boot:run -Dspring-boot.run.profiles=dev",
    "clean": "mvn clean",
    "format": "mvn spotless:apply"
  },
  "keywords": [
    "metadata",
    "schema-registry",
    "spring-boot"
  ],
  "engines": {
    "node": ">=18.0.0",
    "maven": ">=3.8.0",
    "java": ">=21.0.0"
  }
}
