# Development Dockerfile for Message Processor with Hot Reload

FROM maven:3.9-eclipse-temurin-21 AS development

WORKDIR /app

# Copy pom.xml and download dependencies
COPY pom.xml .
RUN mvn dependency:go-offline -B

# Copy source code (will be mounted as volume in dev mode)
COPY src ./src

# Expose debug port
EXPOSE 5007

# Use exec:java for local Flink execution with hot reload
CMD ["mvn", "exec:java", "-Dexec.mainClass=com.beema.processor.MessageProcessorJob"]
