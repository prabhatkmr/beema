apiVersion: v1
kind: Service
metadata:
  name: {{ include "beema-studio.fullname" . }}
  labels:
    {{- include "beema-studio.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "beema-studio.selectorLabels" . | nindent 4 }}
